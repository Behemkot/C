#include <stdio.h>
#include <stdlib.h>

char pierscien1(char l){
    return l;
}

char pierscien2(char l){
    return l;
}

void obroc(char p[]){

}

void szyruj(char t[], rozmiar){
    int i;
    for(i = 0; i < rozmiar; i++){
        t[i] = pierscien2(pierscien1(t[i]));
}

void deszyfruj(char t[]){

}


int main(){


    // czy szyrujemy czy deszyfrujemy

    // wczytujemy tekst
        // usun puste znaki (entery spacje)
        // zmien litery na duże

    // początkowe ustawienie pierscieni

    // jezeli szyfrujemy
    // szyfruj();

    // jeżeli deszyfrujemy
    // deszyfruj();

    // wyświetlamy / zapisujemy do pliku
}
